rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all users collection without authentication
    match /users/{userId}/{document=**} {
      allow read, write: if true;
    }
  }
} 